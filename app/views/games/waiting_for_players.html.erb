<div id="main-content">
  <h1>waiting for more players</h1>
  <!-- display list of players (list will update during polling) -->
  <!-- if player: start polling -->
  <!-- if judge: have a start game button disabled until 3 players or more -->

  <div class="box">
    <h2>Members</h2>
    <% @game.players.each do |player| %>
      <p>
        <span>
          <%= player.handle  %>
          <% if player.judge %>
            &nbsp;&nbsp;&nbsp; -- Game Creator --
          <% end %>
        </span>

      </p>
    <% end %>
  </div>
  <br>
  <br>
  <% if @player.judge %>
    <button onclick='startGame()'>start game</button>
  <% end %>
</div>


<script type="text/javascript">
  var startGame = function(){
    $.get("<%= game_player_round_draw_card_path(round_id: @round.id) %>"), function(data){
      $('#main-content').html(data)
    }
  };
</script>


